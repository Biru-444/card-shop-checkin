# ปัญหาที่พบจากการทดสอบรันแอป

ทดสอบเมื่อ 2026-09-09 โดยรันแอปในโหมดเว็บ (`flet run --web main.py`)
สภาพแวดล้อมที่ทดสอบ: Windows 11, Python 3.11.9, ติดตั้งผ่าน `pip install -r requirements.txt`
ซึ่ง `requirements.txt` ระบุไว้แค่ `flet[all]>=0.28.0` ทำให้ pip ดึงเวอร์ชันล่าสุดตอนติดตั้งมาเป็น
**flet 0.86.5** — เวอร์ชันนี้เปลี่ยน API ภายในไปมากจาก 0.28.x จึงเจอปัญหาด้านล่าง

## 1. [แก้แล้ว] `ft.padding.only` ไม่มีอยู่จริงใน flet 0.86.5 — แอปพัง error เมื่อมีข้อมูลจริง

**อาการ:** เปิดหน้า "วันนี้" หรือ "รายเดือน" ได้ปกติตอนที่ยังไม่มีข้อมูล (แสดงข้อความ "ยังไม่มีข้อมูล")
แต่พอมีการลงทะเบียนอย่างน้อย 1 รายการแล้ว การเปิดหน้าเหล่านี้จะเรียก `game_rows()` ซึ่งมีบรรทัด

```python
padding=ft.padding.only(bottom=14),
```

แล้วแอปจะเด้ง error เต็มหน้าจอทันที:

```
The application encountered an error: module 'flet.controls.padding' has no attribute 'only'
```

**สาเหตุ:** ใน flet 0.86.5 module `flet.padding` (จริง ๆ คือ `flet.controls.padding`) ไม่มีฟังก์ชัน
`only(...)` แบบเก่าอีกต่อไป ต้องสร้างอ็อบเจกต์ `ft.Padding(...)` ตรง ๆ แทน

**การแก้ไข (ทำแล้วใน [main.py:119](main.py#L119)):**

```python
padding=ft.Padding(bottom=14),
```

ทดสอบแล้วว่าใช้งานได้ปกติ (`ft.Padding(bottom=14)` คืนค่า `Padding(left=0, top=0, right=0, bottom=14)`)

**ข้อเสนอแนะเพิ่มเติม (ยังไม่ได้แก้):** ควร pin เวอร์ชัน flet ใน `requirements.txt` ให้ชัดเจน
(เช่น `flet[all]==0.86.5`) เพื่อไม่ให้พังซ้ำถ้า pip ดึงเวอร์ชันใหม่กว่านี้มาในอนาคตที่อาจเปลี่ยน API อีก

## 2. [ยังไม่ยืนยัน] หน้า "ลงทะเบียน" เห็นเป็นกล่องสีเทาทึบตอนทดสอบผ่านเบราว์เซอร์อัตโนมัติ

**อาการ:** ตอนเปิดหน้า "ลงทะเบียน" (checkin_view) ผ่านเบราว์เซอร์ headless ที่ใช้ทดสอบ ส่วนของฟอร์ม
(ช่องชื่อเล่น/เบอร์โทร, checkbox เกม, ปุ่มยืนยัน) เรนเดอร์ออกมาเป็นกล่องสีเทาทึบทั้งกล่อง ไม่มีตัวหนังสือ/label
คลิกแล้วไม่โฟกัส พิมพ์แล้วไม่มีอะไรเกิดขึ้น

ในขณะที่หน้า "วันนี้" และ "รายเดือน" (ซึ่งใช้ `Card`, `Text`, `Container`, `ProgressBar` ธรรมดา ไม่มี
ripple/ink effect) เรนเดอร์ได้ปกติสมบูรณ์ทุกครั้ง — ปุ่ม navigation bar เองก็คลิกเปลี่ยนหน้าได้ถูกต้อง

**ข้อสังเกต:** อาการนี้เกิดเฉพาะ widget แบบมี interactive/ripple state (`TextField`, `Checkbox`,
`FilledButton`) เท่านั้น ส่วน widget แบบ static เรนเดอร์ปกติ — สอดคล้องกับพฤติกรรมที่เคยพบใน
CanvasKit เมื่อรันบน WebGL แบบ software renderer (เช่น SwiftShader ในเบราว์เซอร์ headless/ไม่มี GPU จริง)
ซึ่งบางครั้ง compositing effect อย่าง ripple/elevation จะ paint ผิดเป็นกล่องทึบสีเดียว

**ยังไม่สรุปว่าเป็นบั๊กของแอปจริง ๆ หรือเป็นข้อจำกัดของเครื่องมือทดสอบ (headless browser ไม่มี GPU)**
เพราะเวลาเรียก `Store.add_visit(...)` ตรง ๆ ผ่าน Python (ไม่ผ่าน UI) ข้อมูลบันทึกลง SQLite ถูกต้องสมบูรณ์
(ทดสอบแล้ว: เพิ่ม visit พร้อม 2 เกม แล้ว query กลับมาได้ถูกต้อง) แสดงว่า logic ฝั่ง `Store` ไม่มีปัญหา

**ทางตรวจสอบต่อที่แนะนำ:** เปิดแอปจริงด้วย `python main.py` (โหมดเดสก์ท็อป) หรือเปิดผ่าน Chrome/Edge
ปกติที่มี GPU จริง แล้วดูว่าหน้าลงทะเบียนแสดงช่องกรอกข้อมูลและ checkbox ได้ปกติหรือไม่ ถ้าปกติดี
แปลว่าเป็นข้อจำกัดของเครื่องมือทดสอบเท่านั้น ไม่ใช่บั๊กของแอป
